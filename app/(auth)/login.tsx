import { View, TextInput, Pressable, Text, StyleSheet } from "react-native"; import React,{useState} from "react"; import { useAuth } from "@/providers/AuthProvider"; import Colors from "@/constants/colors";
export default function Login(){const {login,loginGoogle,authLoading}=useAuth(); const [e,setE]=useState(''); const [p,setP]=useState(''); const [err,setErr]=useState(''); const submit=async()=>{try{setErr('');await login(e,p);}catch(ex:any){setErr(ex.message)}}; return <View style={s.c}><Text style={s.h}>Welcome back</Text><TextInput placeholder='Email' placeholderTextColor={Colors.textLight} value={e} onChangeText={setE} style={s.i}/><TextInput secureTextEntry placeholder='Password' placeholderTextColor={Colors.textLight} value={p} onChangeText={setP} style={s.i}/>{!!err&&<Text style={s.err}>{err}</Text>}<Pressable style={s.b} onPress={submit}><Text>Login</Text></Pressable><Pressable style={s.g} onPress={loginGoogle} disabled={authLoading}><Text>Continue with Google</Text></Pressable></View>}
const s=StyleSheet.create({c:{flex:1,padding:20,justifyContent:'center',backgroundColor:Colors.background},h:{fontSize:28,color:Colors.text,fontWeight:'800',marginBottom:16},i:{backgroundColor:Colors.surface,color:Colors.text,padding:14,borderRadius:12,marginBottom:12,borderWidth:1,borderColor:Colors.border},b:{backgroundColor:Colors.primary,padding:14,borderRadius:12,alignItems:'center'},g:{backgroundColor:Colors.surfaceWarm,padding:14,borderRadius:12,alignItems:'center',marginTop:10},err:{color:Colors.danger,marginBottom:8}})
